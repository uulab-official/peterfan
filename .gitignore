# Rust
/target
# Standalone tools excluded from the workspace build their own target dirs.
tools/*/target/
**/*.rs.bk
Cargo.lock.orig

# Release packaging output
/dist

# Local signing/notarization credentials and generated certificate material
.env
private/

# We DO commit Cargo.lock: this workspace produces binaries.
# (Leave the line below commented; documented here so it isn't "fixed" later.)
# Cargo.lock

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp

# Tauri / desktop (planned)
apps/desktop/node_modules/
apps/desktop/dist/
apps/desktop/src-tauri/target/
